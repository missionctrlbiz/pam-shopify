name: Disabled repurpose deploy

on:
  workflow_dispatch:

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - name: GCP deploy parked
        run: echo "GCP deploy is disabled during the Trigger.dev migration."

# GCP Cloud Run deployment is intentionally parked during the Trigger.dev migration.
# Restore the original workflow only if the GCP worker path is revived.
